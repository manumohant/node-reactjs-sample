current directory


	api =>contains nodejs app used as the API
	my-app => contains frontend reactjs app
api/
	npm install -=> to install all packages
	node server.js => to start the api 
my-app/
	npm install => to install allpackages
	npm start => to start the UI


created my-app using command
	npx create-react-app my-app

dependencies
	nodejs

	